name: at_python_interop
description: Dart helper for cross-SDK IV interop tests against the Python SDK.
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  at_client: ^3.11.0
  at_cli_commons: ^2.0.0
  args: ^2.4.0
